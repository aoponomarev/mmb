# План_MBB_to_MMB.md

> Категория: План (высокоуровневая архитектура и стратегия миграции)
> Статус: Активный / Черновик
> Цель: Бесшовный и осознанный перенос логики из MBB в MMB с глубоким рефакторингом.

---

## 🛑 1. Директивы пользователя (Руководство миграцией)
*Этот раздел предназначен для фиксации ваших высокоуровневых указаний и ограничений. Заполняйте, редактируйте и обновляйте его по мере необходимости, чтобы направлять мои действия в процессе работы.*

- **Принцип "По требованию":** Ничего не копируется "про запас". Код, скилы и документация переносятся только тогда, когда они реально нужны в новой архитектуре MMB.
- **Свобода рефакторинга:** Миграция — это не просто copy-paste. Любой переносимый модуль должен быть проанализирован и, при необходимости, переписан с учетом новых стандартов MMB (например, единого `paths.js` и отказа от хардкода).
- **[Ваша директива 1]** *(добавьте сюда свои мысли)*
- **[Ваша директива 2]**

---

## 2. Глобальная стратегия и контекст
Проект **MMB** (`D:\Clouds\AO\OneDrive\AI\PRO\mmb`) является эволюционным развитием **MBB** (`...\Refactoring\ToDo\Statistics\MBB`). 

Вместо того чтобы тащить старый легаси код и неактуальные структуры, мы применяем поэтапный подход:
1. Подготовка чистого фундамента в MMB.
2. Изоляция функционального блока в старом MBB.
3. Его аудит, рефакторинг и интеграция в MMB.
4. Отбрасывание устаревшего (MBB-специфичного) мусора.

---

## 3. Архитектурные парадигмы MMB (в отличие от MBB)
- **Единый источник правды для путей:** Все пути живут в `.env` (только корневые) и вычисляются в `paths.js`. Никакого разрозненного хардкода по файлам.
- **Отказ от `.cursorrules` для логики:** Использование `.mdc` файлов в директории `.cursor/rules/` с явным разделением на `always`, `auto`, `agent` и `manual`.
- **Упрощенная структура скилов:** Один репозиторий, без бессмысленных git-подмодулей, с прямым открытием Obsidian Vault в папке `skills/`.

---

## 4. Этапы миграции

### Этап 1: Фундамент и Интеграция базы знаний (Скилы) — *Текущий фокус*
База знаний должна быть перенесена первой, чтобы AI-агенты работали по новым правилам при миграции всего остального кода.
- [x] Формирование правил и директории `.cursor/rules`.
- [x] Развертывание и адаптация `skills-mcp` (сервера управления скилами).
- [x] Миграция системы хранения ключей и секретов (EIP, preflight-скрипты, `.env.example`).
- [x] Настройка безопасного `config.ts` для Continue (ключи спрятаны в `.env`).
- [ ] Частичная миграция остальных универсальных скилов (процессы, мета-скилы) из MBB.
- 🔗 **Подробный план по этому этапу:** См. файл `docs/План_Skills_MCP.md`.

### Этап 2: Базовая инфраструктура и Control Plane
- [ ] Настройка среды контейнеризации (Docker, `docker-compose.yml`), адаптация конфигураций.
- [ ] Перенос и рефакторинг базовых скриптов автоматизации (Bash / PowerShell).
- [ ] Перенос логики инициализации Control Plane (ядра управления MMB).

### Этап 3: Ядро приложения и Backend
- [ ] Аудит компонентов старого бэкенда MBB.
- [ ] Перенос полезной логики серверов и сервисов в новую структуру.
- [ ] Внедрение системы кэширования и работы с данными, очищенной от MBB-специфики.

### Этап 4: Frontend и UI
- [ ] Перенос и переработка UI-компонентов.
- [ ] Рефакторинг клиентской логики, обновление дизайн-системы и стилей.
- [ ] Связывание интерфейса с новым бэкендом MMB.

### Этап 5: Интеграции и Оркестрация
- [ ] Перенос n8n воркфлоу (если они требуются в новой логике).
- [ ] Настройка деплоя и облачных сервисов (Yandex Cloud и др.).
- [ ] Финальное сквозное тестирование всей инфраструктуры.

---

## 5. Требования к процессу переноса (Чек-лист для агента)
Каждый переносимый из MBB модуль (скрипт, MCP-сервер или UI-компонент) должен пройти через:
1. **Сбор скилов:** Проверка старой базы скилов MBB — какие правила относились к этому модулю.
2. **Адаптация:** Переписывание путей на `paths.js`, замена старых импортов на новые.
3. **Skill Anchors:** Установка `// Skill anchor:` комментариев в перенесенном коде для связи с новыми правилами MMB.
4. **Memory:** Запись важных архитектурных решений по переносу модуля в Memory MCP (`Solution_Pattern` / `Architecture_Decision`).