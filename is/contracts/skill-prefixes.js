/**
 * @deprecated Use is/contracts/prefixes.js (SSOT). Re-exports for backward compatibility.
 */
export {
    SKILL_ALLOWED as IS_SKILLS_MANDATORY,
    SKILL_TYPE_TO_PREFIX as TYPE_TO_PREFIX,
    isValidSkillPrefix as isValidIsSkillsPrefix,
    shouldValidateSkillPrefix as shouldValidateIsSkillsPrefix,
    CORE_RECOMMENDED,
    APP_RECOMMENDED,
} from "./prefixes.js";
