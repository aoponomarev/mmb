# Dependencies
node_modules/

# Secrets — never commit
.env
secrets-backup.txt

# Local data and caches (gitignored per data/README.md)
data/cache/
data/memory-mcp/
data/secret-archives/
data/*.json
data/*.jsonl
data/*.sqlite

# Infrastructure runtime artifacts
is/secrets/archives/
is/logs/*
!is/logs/.gitkeep
is/memory/*.jsonl
is/memory/*.json
is/contracts/.causality-lock.json

# Compromised or local operational files
docs/CRON.txt

# Copilot cron output (generated, not committed)
docs/backlog/copilot-gh/.cron-output.json

# Logs
logs/
*.log

# validate-dead-links --json output (audit artifacts)
dead-*.json

# Encoding scripts output (regenerated on run)
scripts/encoding-fixes/utf8-enforcement-report.json
scripts/encoding-fixes/mojibake-recovery-report.json

# Build artifacts (if any)
dist/
build/
coverage/
.next/
out/

# System
.DS_Store
Thumbs.db

# IDE / AI
.continue/
.cursor/debug-*.log
